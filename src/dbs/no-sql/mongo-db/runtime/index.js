'use strict';

/**
 * MongoDB-specific runtime helpers (if any).
 * MaskDatabase/MaskModels in src/runtime are shared; DB-specific behavior
 * is in the generated queries.js and models.js.
 */
module.exports = {};
